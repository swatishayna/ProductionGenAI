[project]
name = "production-genai"
version = "0.1.0"
description = "My Production RAG"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    "fastapi[standard]>=0.115.12",
    "sqlalchemy>=2.0.0",
]
